image: ubuntu
stack: jdk 11
branches:
  only:
    - main
build: off
install:
  - chmod +x ./artifacts/chromedriver
  - java -jar ./artifacts/app-order.jar &
  - chmod +x gradlew
build_script:
  ./gradlew test -Dselenium.headless=true --info
